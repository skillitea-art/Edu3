name: vedo
description: "Smart Tuition Study & Feedback App"
publish_to: 'none'

version: 1.0.3+4

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^2.25.4
  firebase_auth: ^4.17.5
  cloud_firestore: ^4.15.5
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  intl: ^0.18.0
  package_info_plus: ^4.0.0
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  firebase_core_platform_interface: ^5.4.2

flutter:
  uses-material-design: true
